# Pyground
Python Playground

## Basic Git Commands

    #git push process
git add .
git commit -m"MESSAGE"
git push

 ## next one needs verification
git branch []

#looks at, and moves to branch
git branch -b[]